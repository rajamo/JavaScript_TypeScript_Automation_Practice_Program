//find the count of character occureance 'Geek'

let letter:string = 'geek';
let countArray: Array<any> = [];
for (let i=0; i<letter.length; i++)
    {
    countArray.push(letter[i]);
    console.log(countArray[i]);
    // const count = (countArray:any):Record<string, number> => 

     
};
